# params.yaml — Hiperparâmetros e configurações do pipeline DVC
# Referenciar com: dvc params show

prepare:
  test_size: 0.2

train:
  model_type: item_knn
  top_k: 10
  similarity: cosine

evaluate:
  k: 10
  metrics:
    - precision_at_k
    - recall_at_k
    - map_at_k
    - ndcg_at_k
